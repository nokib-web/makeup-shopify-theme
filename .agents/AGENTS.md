# Workspace Rules

- Never run commands (especially destructive or publish/push commands like `shopify theme push`) or push code to production/remote without explicit permission from the user. Always ask for permission before executing commands that modify remote state.
